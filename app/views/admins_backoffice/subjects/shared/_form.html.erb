<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header"><%= "#{action_message} Assunto/Área" %></h1>
    </div>
</div>

<%# if @subject.errors.any? %>
<%# end %>

<%= form_with(model: [ :admins_backoffice, @subject ], local: true) do |form| %>
    <div class="form-group">
        <%= form.label :description %>
        <%= form.text_field :description, class:"form-control", placeholder: "Descrição do Assunto/Área" %>
    <div id="emailHelp" class="form-text">Não compartilhe suas informações com outra pessoa!</div>
    </div>
    <button type="submit" class="btn btn-dark">Salvar</button>
<% end %>